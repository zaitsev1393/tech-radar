export const RADAR_WIDTH = 800;
export const RADAR_HEIGHT = 800;

export const DEFAULT_RADAR_CONFIG = {
  id: "radar",
  width: RADAR_WIDTH,
  height: RADAR_HEIGHT,
};

export const DEFAULT_SVG_CONTAINER_CONFIG = {
  ...DEFAULT_RADAR_CONFIG,
  id: "svg",
};
