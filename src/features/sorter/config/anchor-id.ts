/**
 * The anchor ID used for the sorter feature.
 * This constant is typically used as an HTML anchor or identifier
 * within the application to reference the sorter section.
 */
export const ANCHOR_ID = "sorter";
